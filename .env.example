# Phase 2 placeholders. Keep real secrets in your local .env only.
# Note: EXPO_PUBLIC_* values are bundled into the client app.
# Gemini now runs through the Supabase Edge Function `gemini-proxy`.
# Store the real GEMINI_API_KEY in Supabase secrets, not in this client .env.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
